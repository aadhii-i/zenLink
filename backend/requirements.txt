# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database (async) ---
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Cache ---
redis==5.2.1

# --- Auth ---
# Note: bcrypt is used directly (not via passlib) — passlib's bcrypt backend
# is incompatible with bcrypt>=4.1 and the project is unmaintained.
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# --- Rate limiting (Redis-backed, see core/rate_limit.py) ---
slowapi==0.1.9

# --- Utilities ---
user-agents==2.2.0

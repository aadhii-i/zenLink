# Dev/test-only dependencies — NOT installed in the production Docker image
# (see docker/backend.Dockerfile's production stage). Install alongside
# requirements.txt for local development and CI:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1

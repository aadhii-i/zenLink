# Copy to frontend/.env for running `npm run dev` directly on your host
# (outside Docker).
#
# When running via `docker-compose up`, this file is NOT used — the
# frontend container gets VITE_API_URL from the project ROOT .env instead.

VITE_API_URL=http://localhost:8000/api/v1
